const fs=require('fs');
module.exports={
    permit:function(id,pName){
        var obj=JSON.parse(fs.readFileSync(__dirname+'/config.json'));
        var per=obj['permit'];
        if(per[pName].indexOf(id)!=-1){
            return true;
        }
        return false;
    }
};